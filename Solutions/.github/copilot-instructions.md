# Copilot Instructions

## Project Guidelines
- In the DigitalBusiness repo, the actual Git repository root is C:\Users\david\dev\repos\DigitalBusiness, while the IDE-reported workspace root (...\Solutions\) can differ/contain shadow paths. File edit/create/delete operations should target the real repo root, and if relative-path file tool calls intermittently fail to match, retry using the absolute path.